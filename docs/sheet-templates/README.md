# Google Sheet 복붙 템플릿

## 사용 방법
1. 구글 시트에서 `wbg_member`, `wbg_history` 탭을 만든다.
2. 각 탭의 `A1` 셀 클릭 후, 아래 파일 내용을 통째로 복붙한다.
   - `wbg_member.tsv` -> `wbg_member` 탭
   - `wbg_history.tsv` -> `wbg_history` 탭
3. 첫 행은 헤더이므로 유지한다.

## 메모
- 파일은 탭 구분(TSV)이라 엑셀/구글시트에 그대로 들어간다.
- 샘플 1행은 테스트용이다. 원하면 지워도 된다.
